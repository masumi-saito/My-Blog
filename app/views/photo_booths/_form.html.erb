<%= form_with(model: photo_booth, local: true) do |form| %>
  <% if photo_booth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo_booth.errors.count, "error") %> prohibited this photo_booth from being saved:</h2>

      <ul>
        <% photo_booth.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.text_field :photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
